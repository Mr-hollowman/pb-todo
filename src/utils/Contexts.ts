import { createContext } from "react";
const ToastContext = createContext({});
const UserContext = createContext({})
const TodoContext = createContext({})
export { ToastContext, UserContext, TodoContext };
